# Azure-Data-Factory-Project

The idea of the project is to create a pipeline connecting Github with Azure Data Factory, creating new branches and generating the ARM template. The specific branch where the template is published is the **adf_publish** branch having the final json file.
